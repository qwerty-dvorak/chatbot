- Remove all the mock server folders 
- Make a dir at root which is called models, the directory should only include shell scripts with docker commands or runpod commands, 2 shell scripts one for vllm gemma inference server, mount the file in ../../models/gemma (this is out of the git repo itself, which is intended) another shell script for container for rag text embed, rag mm embed and rag reranker, runpod should just run all pods from single script and move all runpod shell scripts there, delete all runpod wait script 
- In rag pipeline folder and chatbot service folder have a test dir move all python test there, move all bash files to shell scripts folder
- Have only a single .env.runpod in models folder which had endpoints for runpod, have a single .env.local in models which has all the endpoint for localhost with port, only modal difference will be in runpod i am running google/gemma-4-E4B-it but in vllm i am running google/gemma-4-26B-A4B-it, handle that and expose that in env
- Have a single common postgres db in docker file in new db folder in root, it should be exposed through ip and port, handle add migration in chatbot and db queries from chatbot only, in root keep shell script to clear postgres volume ie all data, always check for migration in chatbot, dont be afraid to clear all data, have sanity check scripts and health check scripts as well as docker exec in container to run sql queries, use port 5433
- Have a single common milvus with minio and etcd in new milvus folder, it should also be exposed through ip and port, handle everything about milvus in rag pipeline only, similiar option to clear and view stuff, write shell scripts to exec commands in docker containers to check contents in minio etc
- in chatbot folder have a script which loads env var of models from either models/.env.local or models/.env.runpod, after that it runs all the web services through a shell script, use data folder at root of repo to store file server data 
- do similiar as above in rag pipeline folder also
- make the entire structure more cleaner and more modular
- make seed_data folder at root, for now just add a .gitignore in it, make a sample_data folder at root it should have 3 sub folders text, image, pdf move the rag pipeline sample data in the respective folders
- have 3 separate test suits, one for chatbot main, one for rag pipeline and one for integration, make test folder at root for integration tests, setup up docker file for each test suit, it should run the containers for respective subsystem and then have unit test for each feature eg tool call in chatbot, image attachment in chatbot, hyde in rag flow, subquery in rag flow, have seed_data in rag as needed, write test cases for ingestion too, do api level test also, in the end add integration test which use both things, like the chat with rag call
- time each api request and always have that in all the requests, also time model requests, dont time db calls but time major pipeline steps in rag 
- make a tool for user memory, aggregate it from all the chats and make it dynamicaly update, each tool call will for now be an internal api call or maybe a subprocess run on the server itself
